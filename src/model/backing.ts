export class BadgerBacking {
    public readonly company: string;
    public readonly amount: number;

    public constructor(company: string, amount: number) {
        this.company = company;
        this.amount = amount;
    }
}